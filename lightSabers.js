//write a function with conditonals to test how many light sabers each person has

// created a function with a parameter called name
function howManyLightsabersDoYouOwn(name) {
  //if conditionals with the parameter if the name is zach it returns 18 sabers and the rest will return 0
  if( name === "Zach"){
    return 18
  }else {
    return 0
  } 
}
